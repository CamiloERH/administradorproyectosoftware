import { CREAR_PRODUCTO } from "../../types";

const productosReducer = (state, action) => {
    switch(action.type){
        default:
            return state;
    }
}

export default productosReducer;